# CLAUDE.md — 우리동네 보험소 (프로젝트 마스터 규칙)

> 이 파일은 매 작업 시작 시 반드시 읽고 준수한다.
> 아래 규칙과 충돌하는 요구가 들어오면 **구현을 멈추고 이 문서를 근거로 플래그**를 띄운다.

---

## 0. 프로젝트 개요
보험이 막막한 사람(가족 사망·상속, 암 진단, 청구 거절 등)을 **가까운 보험 전문가**와
잇는 **위치기반 플랫폼**. 보험을 파는 곳이 아니라, 소비자가 전문가를 **직접 골라 연결**하는
"광고/정보 플랫폼"이다.

**현재 단계: 풀앱(v2) 운영 준비.**
- 제품 = 기존 풀앱(`bohumso-netlify`) 유지: 네이비 프리미엄 디자인 · 지도 · 회원가입/로그인 · 상담 탐색 · 결제 전부 있음.
- 디자인은 `design-system.md`(네이비 토큰) 가드레일을 지켜 **깨지지 않게**만 관리한다.
- **현행 코드에서 반드시 바꿀 2가지(§1·§2 준수):**
  1. **자동매칭 제거 → 소비자가 목록에서 직접 선택만.** (rank/offer/matching-sweep·자동매칭 동의 흐름 제거)
  2. **건당 과금 제거 → 설계사 정기결제(구독) 방식.** (consultation 건당 결제 → 노출 슬롯 구독)

---

## 1. 절대 원칙 — 합법 구조 (위반 불가)
이 서비스의 합법성은 아래에 달려 있다. 어떤 이유로도 넘지 않는다.

1. **소비자가 먼저 움직인다.** 소비자가 목록에서 전문가를 직접 선택하고, 본인이 연락을 개시한다.
2. **고객 정보를 넘기지/팔지 않는다.** 등록 정보를 여러 전문가에게 자동 전송·분배·판매하지 않는다.
3. **연결에 과금하지 않는다.** 수익은 정액(노출·구독·입점료)뿐. "연결/성사/연락처"를 트리거로 한 과금 금지.
4. **지급 보장 표현 금지.** "무조건 받아드립니다" 류 확정 표현 금지. "전문가가 함께 확인/안내" 톤만.

### 절대 만들지 말 기능 (요청받아도 거부하고 플래그)
- 자동 매칭·1:1 추천·강제 배정 (목록 제공만 허용)
- 소비자 정보를 전문가에게 push / 전문가가 pull 하는 리드 전달
- 연결·성사·계약·연락처 조회에 연동된 과금/차감/정산
- 성공보수(%) 배분 로직

---

## 2. 과금 규칙 (앱 단계에서 적용)
- 과금 트리거 = **선불 결제(구독 시작 / 노출 크레딧 충전)** 뿐
- 환불 트리거 = **약정 노출·조회 미달(플랫폼 귀책)** 뿐. "연결수"는 계산식에 넣지 않는다
- 연결/연락 개시 건수는 **대시보드 표시(display-only)** 만. 과금·환불·정산과 절대 연결 금지
- 요금제 = 노출 슬롯 구독(베이직/프리미엄/지역독점) 정액

## 3. 명명 규칙 (코드·DB·UI·문구 전체)
감사 시 코드가 증거가 된다. 용어를 통일한다.
- ❌ 금지: `lead`, `리드`, `DB판매`, `건당`, `per_connection`, `성사`, `소개비`, `알선`, `중개수수료`
- ✅ 사용: `impression`(노출), `profile_view`(조회), `ad_subscription`(광고구독),
  `exposure_credit`(노출크레딧), `slot`(노출슬롯), `guaranteed_impressions`(약정노출수)

---

## 4. 디자인 시스템 강제 (모든 UI 작업)
색/폰트/간격/radius는 `design-system.md`의 토큰만 사용한다. 값 하드코딩 금지.

- 색(네이비): `--brand #1a56db`(주요/링크) · `--navy #101e36`(딥 배경) · `--accent #477cff`(강조) ·
  `--bg #f4f5f8`(배경) · `--card #fff` · `--text #111827` · `--muted #6b7280` · `--border #ecedf1`
- 폰트: 시스템 산세리프 스택(별도 웹폰트 미로드)
- radius = 기본 18(버튼 13, 입력 10~12). 그림자·간격은 `design-system.md` 스케일 사용
- 버튼·카드·입력창·바텀시트·탭바는 `design-system.md`/`styles.css`의 **기존 컴포넌트만 재사용**. 매번 새로 만들지 말 것

### ⚠️ UI 작업 종료 전 자가검사 (하나라도 실패면 수정 후 종료)
1. 모든 색이 토큰인가 (하드코딩 hex 없음)
2. 텍스트 대비 4.5:1 이상인가
3. 간격/ radius가 스케일 위에 있는가
4. 모바일 360px에서 안 깨지는가 (가로 스크롤·잘림 없음)
5. 버튼 글자가 배경에 잘 보이는가, 포커스 표시가 있는가

---

## 5. 기술 스택 (현행)
- 정적 HTML/CSS/JS + esbuild → Netlify 배포(Functions 포함), Supabase(인증·DB), 토스(결제).
- 결제는 현재 상담 건당(토스 일반결제) → **정기결제(구독)로 전환 중**(§2). 라이브 키 차단·테스트 모드 유지.
- 지도: OpenStreetMap 타일(현재) / 카카오맵 전환 검토 가능.

## 6. 작업 습관
- 큰 변경 전 계획을 먼저 말하고, 합법 원칙(1장)과 충돌 없는지 스스로 점검
- 기능 단위로 작게 커밋, 각 커밋 후 4장 자가검사 수행
