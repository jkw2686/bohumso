SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
OPERATOR_NAME=
PRIVACY_CONTACT=
POLICIES_APPROVED=false
ACCOUNTS_ENABLED=false
# Test-only payment settings. Leave secrets blank; put them in server environment variables.
TOSS_MODE=test
PAYMENTS_ENABLED=false
TOSS_CLIENT_KEY=
TOSS_SECRET_KEY=
SUPABASE_SERVICE_ROLE_KEY=
APP_ORIGIN=http://localhost:3100
VISIT_METRICS_ENABLED=false

AD_EXPOSURE_ENABLED=false
AD_IMPRESSION_SECRET=

# Enable only after custom:naver registration, userinfo deployment and real callback verification.
NAVER_LOGIN_ENABLED=false

# Isolated admin-only test orders; requires migration 010.
ADMIN_TEST_PAYMENTS_ENABLED=false

# Keep disabled until private bucket + migration 011 + SMS setup are verified.
EXPERT_DOCUMENTS_ENABLED=false
