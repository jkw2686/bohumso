import http from 'node:http';import path from 'node:path';import {readFile,mkdir} from 'node:fs/promises';import {build} from 'esbuild';
export async function startExpertPreview(port=3192){
 await mkdir('artifacts',{recursive:true});await build({entryPoints:['tests/fixtures/expert-preview.js'],bundle:true,format:'esm',outfile:'artifacts/expert-preview.js'});
 const html='<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>전문가 가입 · 로컬 가상 미리보기</title><link rel="stylesheet" href="/styles.css"><link rel="stylesheet" href="/account.css"><link rel="stylesheet" href="/expert-onboarding.css"></head><body><nav class="nav"><a class="logo" href="/"><span class="local">우리곁에</span><span class="brand">보험소</span></a></nav><main class="container"><p class="card">로컬 가상 체험 · 저장·문자 발송 없음<br>가상 정보와 가상 서류만 사용하세요. 인증번호 123456<br><a href="/">가입</a> · <a href="/?view=admin">관리자</a> · <a href="/?view=badge">서약 뱃지</a></p><h1>전문가 가입</h1><p id="partnerState"></p><form id="partnerForm" class="card" hidden></form><div id="partnerDetails" hidden></div><div id="demoHost"></div><p id="accountMessage" role="alert"></p></main><script type="module" src="/expert-preview.js"></script></body></html>';
 const server=http.createServer(async(req,res)=>{const url=new URL(req.url,'http://localhost');try{let data,type='text/html; charset=utf-8';if(url.pathname==='/')data=html;else if(url.pathname==='/expert-preview.js'){data=await readFile('artifacts/expert-preview.js');type='text/javascript';}else if(['/styles.css','/account.css','/expert-onboarding.css'].includes(url.pathname)){data=await readFile(path.join('public',url.pathname));type='text/css';}else{res.writeHead(404).end();return;}res.writeHead(200,{'Content-Type':type,'Cache-Control':'no-store'}).end(data);}catch{res.writeHead(500).end();}});
 await new Promise(resolve=>server.listen(port,'127.0.0.1',resolve));return server;
}
if(process.argv[1]?.endsWith('expert-preview.mjs')){await startExpertPreview();console.log('Local mock only: http://127.0.0.1:3192/');}
