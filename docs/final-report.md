# 작업 결과 — 2026-09-20 광고 구독 모델 전환

## 구현
소비자 직접 선택만 허용합니다. 상담에서 가격·무료 이용권·단계별 주문·미결제 수락 제한을 제거했습니다.
상담 확정·완료·취소·분쟁은 광고 구독과 별개로 처리됩니다. 연락처는 기존 동의·역할 격리를 유지합니다.

새 ad_plans, ad_slots, ad_subscriptions, ad_impressions, ad_audit와 제한된 RPC를 사용합니다.
초기 세 요금제는 미정 조건을 NULL로 유지하고 비활성입니다. 관리자가 새 조건 버전을 저장하면 이전 버전 판매는 중지되며 기존 주문 조건은 바뀌지 않습니다.
광고 구독은 기간제 선불·수동 갱신입니다. 자동결제 빌링은 구현하지 않았습니다.
지역독점 슬롯 잠금, 중복 요청 키, 승인 키·금액 검증, 중복 승인 시 기간 유지, 환불 후 지연 승인 무시를 적용했습니다.
환불은 기간 종료·약정 노출 미달·관리자 플랫폼 귀책 근거가 있어야 요청할 수 있고 감사 이력을 기록합니다. 전액 환불만 구현했습니다.

광고 영역은 일반 목록과 분리해 표시합니다. 서버 서명 토큰, 10분 만료, 이벤트 중복 방지, 기본 비활성 수집 설정을 적용했습니다.
노출 집계는 실사용자 인증/봇 판별을 의미하지 않습니다. 운영 수준 부정 조회 방어는 추가 검토가 필요합니다.

## 화면
기존 네이비 컴포넌트를 재사용합니다. 설계사·관리자 화면에 구독 조건, 지역 슬롯, 기간·노출 현황, 상태 재조회·환불을 추가했습니다.
결제 모달은 로딩·실패·닫기·재열기·키보드 포커스와 모바일 스크롤을 처리합니다.
토스 위젯용 테스트 키(test_gck_/test_gsk_)로 수정했습니다. 카드가 없는 간편결제 잔액 응답도 검증 가능하며 라이브 키는 차단됩니다.

## 검증 범위
PGlite DB, 모의 Supabase 인증·PG, Edge 기반 테스트입니다. artifacts/verification-report.json의 최신 실행 시각과 passed를 확인하세요.
테스트: 상담/과금 독립, 미정 상품 차단, 역할 격리, 가격 고정, 독점 슬롯, 만료, 중복 승인, 노출 중복, 환불 조건·재시도, 위조 토큰·출처 차단, 모바일 결제 모달 재열기와 관리자 흐름.
실제 PostgreSQL 다중 연결은 npm run concurrency로 별도 확인하며 실행 여부를 모의 테스트와 혼동하지 않습니다.

## 미완료·외부 검증
실제 Supabase 이메일/권한, 토스 테스트 상점 승인·취소·웹훅, 운영자·자격 확인, 요금/약정 조건·약관·환불/보유기간 법률 검토.
저장 카드 자동 갱신, 부분 환불, 노출 크레딧 충전형 상품은 미구현입니다.
기존 003/004 적용 DB용 전환 마이그레이션·거래 보존 이관은 별도 작업입니다. 현재 SQL 합본은 새 빈 테스트 DB용입니다.
실배포·실결제 없음. 운영 준비가 모두 완료된 상태가 아닙니다.
